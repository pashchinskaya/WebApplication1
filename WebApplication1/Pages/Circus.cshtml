@page
@model CircusModel
@{
    ViewData["Title"] = "Цирк";
    ViewData["ShowImage"] = false;
}

<h1>@ViewData["Title"]</h1>

@if (Model.CircusEvents != null && Model.CircusEvents.Any())
{
    <table class="table">
        <thead>
            <tr>
                <th>Название</th>
                <th>Описание</th>
                <th>Дата</th>
                <th>Цена</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.CircusEvents)
            {
                <tr>
                    <td>@item.Title</td>
                    <td>@item.Description</td>
                    <td>@item.DateEvent.ToString("dd.MM.yyyy HH:mm")</td>
                    <td>@item.TicketPrice</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>Нет цирковых представлений в базе данных.</p>
}


